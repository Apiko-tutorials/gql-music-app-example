import { track, tracks, album, albums } from './records';
import { artist, artists } from './artists';
import { search } from './search';

export const queryResolvers = {
  track,
  tracks,
  album,
  albums,
  artist,
  artists,
  search,
};
