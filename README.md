### GraphQL music app example

#### Getting Started
* clone repo
* start `mongod` process if needed
* navigate to repo folder and install dependencies: `yarn install`
* start express server with `yarn start`
* visit http://localhost:3000/graphiql
